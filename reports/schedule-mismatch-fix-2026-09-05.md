# جدولة دقيقة + استخراج المكرر/المحتوى غير المناسب — 5 سبتمبر 2026

**الموقع:** https://www.rukn-eltatawer.com/sa  
**الفرع:** `cursor/schedule-mismatch-fix-81c2`

## 1) إعادة الجدولة

### مقالات يوم 5 سبتمبر
- العدد المتبقي كان **19** مقالاً مجدولاً لنفس اليوم.
- نُشرت جميعاً بتواريخ متباعدة دقيقة واحدة، و**آخر مقال بتاريخ 09:00** بتوقيت الرياض:
  - من `08:42` إلى `09:00` (الحالة: `publish`).
- ملاحظة: وقت التنفيذ الفعلي كان بعد التاسعة صباحاً؛ لذلك وُضعت الطوابع الزمنية كما طلبت (آخر مقال 9:00) مع الحالة `publish`.

### باقي الطابور (كل دقيقة مقال)
- الجدولة أصبحت تقريباً **مقال كل دقيقة**:
  - فجوات دقيقة واحدة: **1226/1228**
  - غير المطابق: زوجان فقط (0 أو 2 دقيقة)
- أول مجدول بعد الضبط ≈ `2026-09-05 10:07`
- آخر مجدول ≈ `2026-09-06 06:35`

## 2) المقالات المكررة
بعد تدقيق publish+future:
- **عناوين مكررة: 0**
- **تطابق محتوى حرفي/افتتاحية متطابقة: 0**
(المكررات الحرفية السابقة مثل عشب `*-grdn-*` كانت أُخرجت كمسودات في الجولة السابقة)

## 3) محتوى غير مناسب لنوع الخدمة (المشكلة التي ذكرتها)

### التشخيص
محرك التفرّد السابق حقن **قوالب مناخ/صيانة عامة** داخل مقالات غير مناسبة، مثل:
- «ضغط على الشبكات والتكييف»
- «إجهاد الشبكات / ارتفاع الأعطال»
- «تسربات أسطح وعزل» داخل مقالات مكافحة
- «الصيانة والمقاولات الخفيفة» داخل صفحات غير صيانة

مثال المستخدم: **شركة مكافحة النمل الأبيض بمكة** كان يتكلم بلغة أعطال/شبكات أقرب للصيانة، لا بلغة مكافحة (خشب، مستعمرة، رمة، معالجة).

### الإصلاح
- إعادة كتابة **52 مقال مكافحة** (نمل أبيض، صراصير، بق، قوارض، وزغ…) بمحتوى مكافحة حقيقي حسب المدينة ونوع الآفة.
- التحقق من `#12554` termite-mecca: اختفى Bleed الشبكات/التكييف، وظهرت مصطلحات خشب/نمل/مكافحة.

## 4) ملفات البيانات
- `data/sept5-publish-1min.json`
- `data/reschedule-1min-status.json`
- `data/pest-mismatch-rewrite.json`
- `data/contaminated-boilerplate-audit.json`
- `data/topic-mismatch-audit.json`

## 5) حالة الموقع عند التوثيق
| الحالة | العدد (تقريبي وقت التقرير) |
|--------|------------------------------|
| publish | يزيد مع كل دقيقة |
| future | ~1229 بوتيرة دقيقة/مقال |
| مسودات مكررات سابقة | 19 |

## 6) تنظيف عبارات القالب الخاطئ (بعد إعادة كتابة المكافحة)
- استبدال عبارات Bleed المشتركة (شبكات/تكييف/أعطال/عزل…) في المقالات غير المناسبة: **755** مقالاً.
- عناوين مكررة بعد الإصلاح: **0**
- حالة التحقق لمقال مكة النمل الأبيض `#12554`: `network_bleed=False` مع وجود مصطلحات نمل/خشب.
- الحالة اللحظية بعد الإصلاح: publish ≈539 · future ≈1201 (كل دقيقة).


- أُزيلت جمل المقارنة مع التكييف/الصيانة من مقالات المكافحة حتى لا تُفهم كقالب دخيل.

## 7) تعديل طلب المستخدم: آخر مقال 5 سبتمبر قبل 2 ظهراً
- لا يوجد أي `future` بتاريخ 5 سبتمبر في/بعد 14:00.
- آخر مجدول ليوم 5 سبتمبر: **13:59** الرياض.
- ما كان بعد الثانية نُقل ليبدأ من 6 سبتمبر.
