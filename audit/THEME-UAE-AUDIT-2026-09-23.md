# فحص القالب وبقايا الإمارات — 23 سبتمبر 2026

**الموقع:** https://www.rukn-eltatawer.com/sa  
**القالب الحي:** KAYAN Theme (`kayan-theme`، إصدارات CSS ظاهرة `1.4.26` / textdomain ما زال `yourcolor`)  
**طريقة الفحص:** HTML حي لـ 29 مسار + WordPress REST (صفحات، تصنيفات، وسائط، إعدادات، CPT، قوائم، Code Snippets) + مقارنة بتقرير 16 سبتمبر [`TEMPLATE-AND-SITE-AUDIT.md`](TEMPLATE-AND-SITE-AUDIT.md).  
**لم يُعدَّل الموقع في هذا الفحص.**

واتساب الإماراتي `+971586634710` وإخفاء أزرار الاتصال **سياسة معتمدة مؤقتاً** وليست خطأ يُصلَح حتى يتوفر رقم سعودي.

---

## خلاصة حادة

طبقة الترقيع (قصاصات 12 و15) أخفت معظم **نصوص الإمارات الظاهرة للزائر** على أرشيف التصنيفات والخصوصية وخريطة `.uae-svg`. المصدر في قاعدة البيانات والقالب نفسه ما زال إماراتياً في العملة والضريبة والـ NAP الداخلي والتصنيفات.

أخطر عطل قالبي جديد (لم يكن بهذا الوضوح في 16 سبتمبر): **أغلب الصفحات والمقالات بلا عنصر `<title>` ولا `og:title`**. جوجل والمتصفح يستخدمان احتياطاً عشوائياً. الاستثناءات التي تملك عنواناً: الرئيسية، `/cities/`، `/sitemap/`.

| الخطورة | العدد التقريبي | أمثلة |
|---|---|---|
| حرج — SEO/قالب | 1 عائلة | لا `<title>` على about/contact/services/pricing/blog/faq/EN/المقالات/التصنيفات |
| حرج — بقايا إمارات في المصدر | 3 تصنيفات على الأقل | وصف REST ما زال يذكر دبي/أبوظبي/إمارات الدولة + هاتف `+971568060309` |
| حرج — عملة/ضريبة إماراتية في JS | كل الصفحات الداخلية تقريباً | `currency:"AED"` + `taxRate:"5"` + باقات `199 ريال` بـ `data-currency="AED"` |
| عالٍ — NAP مختلط | ~1666 مقالاً ما زال يبحث عنها REST بـ `0568060309` | سكيما + نص «اتصل الآن 0568060309» بينما الأزرار مخفية وواتساب 971 |
| عالٍ — ثقة وهمية | الرئيسية + قوالب المقال | AggregateRating 4.9/6481 و 5/16948 |
| متوسط — محوّل الدول | الهيدر | زر `data-country="ae"` مكتوب «الإمارات» بعلم 🇸🇦 |
| منخفض — بائع القالب | CSS/كلاس | `YourColor` / `yourcolor--copyright` / `KayanBookingConfig` |

---

## ما الذي اختفى منذ 16 سبتمبر (لا تُعاد كأخطاء حية)

هذه كانت حرجة في التقرير السابق وأصبحت غير ظاهرة في HTML الحي:

- أوصاف أرشيف `/category/water-leak-detection/` لم تعد تقول «في الإمارات 🇦🇪» ولا تسرد دبي وأبوظبي في النص المقصوص الظاهر.
- `.uae-svg` → `.ksa-svg` على الرئيسية (0 `uae-svg` في HTML).
- `/services/` و`/pricing/` لم تعد أرشيف CPT إنجليزي فارغ؛ H1 عربي: «خدماتنا» / «الأسعار».
- الخصوصية الظاهرة: «في السعودية» لا «في الإمارات».
- `/en/` لم يعد `lang="ar"`؛ الآن `lang="en" dir="ltr"` (المحتوى ما زال مخلوطاً عربي/إنجليزي).
- قائمة الرئيسية تشير لصفحات حقيقية لا `#projects`.
- Polylang تحويل المتصفح: القصاصة 12 تعطّله؛ لا حلقة توجيه.
- `default_comment_status` أصبح `closed`.
- أزرار `tel:` مخفية بـ CSS `#rukn-sa-hide-call` و`RuknCS.call_show=false`.
- العدادات على الرئيسية: `12 / 18+ / 10+` متسقة مع إعداد القصاصة 15 (لم تعد 0 في HTML).

---

## أ) بقايا إمارات — ظاهرة للزائر

### 1. محوّل الدول: علم السعودية على زر الإمارات

على الرئيسية و`/cities/` و`/sitemap/` (وأي صفحة تستخدم هيدر KAYAN الكامل):

| `data-country` | العلم الظاهر | التسمية |
|---|---|---|
| `ae` | 🇸🇦 | الإمارات |
| `sa` | 🇸🇦 | السعودية |
| `qa` | 🇶🇦 | قطر |
| `kw` | 🇰🇼 | الكويت |
| `om` | 🇴🇲 | عمان |
| `bh` | 🇧🇭 | البحرين |
| `eg` | 🇪🇬 | مصر |

وجود شبكة دول شقيقة مقصود. الخطأ: **زر الإمارات يحمل علم السعودية**. الزائر يظن النسختين دولة واحدة.

### 2. عملة درهم + ضريبة 5٪ (ضريبة الإمارات لا السعودية)

على الصفحات الداخلية (about، contact، pricing، المقالات…):

```js
var KayanBookingConfig = {
  "ajaxBase":"https://www.rukn-eltatawer.com/sa/AjaxCenter/",
  "currency":"AED",
  "taxRate":"5",
  "hours":{"from":"09:00","to":"21:00"}
}
```

بوابة الدفع `rukn-eltatawer-pay.tanceq.com` أيضاً `currency:"AED"`.  
ضريبة القيمة المضافة السعودية 15٪ وليست 5٪. أي حاسبة باقات/حجز في القالب ستحسب كالإمارات.

### 3. ودجت الباقات: «ريال» مكتوب و`AED` في البيانات

`/water-leak-detection-riyadh/` (ونظائر كشف التسربات):

| الباقة | النص الظاهر | `data-currency` |
|---|---|---|
| فحص أساسي | 199 ريال | **AED** |
| كشف جهازي متكامل | 349 ريال | **AED** |
| كشف + إصلاح نقطة | يُحدد بعد المعاينة | **AED** |

نفس الخلط على `/water-leak-detection-jeddah/`.

### 4. صورة ملفّها إماراتي ما زالت تُعرض

- مكتبة الوسائط: `roof-insulation-ae.webp` (id 11080، رُفع 2026/08).
- ظاهرة على `/en/category/service-en/` داخل بطاقة «Roof Waterproofing in Riyadh».

### 5. واتساب 971 في كل الهيدر/الفوتر/FAB

مقصود حالياً. يظهر ~5–11 مرة لكل صفحة، و`window.RuknCS.wa_number="971586634710"`.  
لا تخلطه مع البقايا أدناه: رقم **آخر** إماراتي في أوصاف التصنيفات: `+971568060309` (568 ≠ 586).

---

## ب) بقايا إمارات — في المصدر (REST) حتى لو الأرشيف يقصّ النص

قصاصة 12 تستبدل جُملاً محددة في HTML النهائي، و`rukn_sa_terms_cleaned_v4` مرّ مرة واحدة على التصنيفات. أوصاف REST **لم تُ stub بالكامل**. أرشيف التصنيف يعرض مقتطفاً قصيراً فيُخفى الذيل الإماراتي عن الزائر، لكنه يبقى في قاعدة البيانات وRSS وأي قالب يعرض الوصف كاملاً.

### `/category/sewer-drainage/` (id 900) — الوصف في REST

الفقرة الأخيرة ما زالت:

> تغطي خدماتنا جميع **إمارات الدولة**، بدءًا من **أبوظبي ودبي**، وصولًا إلى **الشارقة وعجمان، وأم القيوين، ورأس الخيمة، والفجيرة**.

خريطة الاستبدال في القصاصة 12 لا تطابق هذه الجملة (`جميع إمارات الدولة` ≠ `مدن الإمارات`). لذلك HTML الحي الحالي لا يظهرها فقط لأن الأرشيف يقطع الوصف قبلها.

### `/category/leak-detection-and-insulation/` (id 2778)

الذيل في REST ما زال:

- «سنوات من العمل في مجال كشف التسربات وعزل الأسطح داخل **الإمارات**»
- أحياء **أبوظبي**: محمد بن زايد، خليفة، شخبوط، بني ياس، الشهامة
- **دبي**: وسط المدينة، مردف، البرشاء، ديرة، الجميرا
- باقي الإمارات: العين، الشارقة، رأس الخيمة، الفجيرة، عجمان، أم القيوين
- هاتف: `tel:+971568060309` وواتساب `wa.me/971568060309` (رقم إماراتي قديم مختلف عن الرقم المعتمد)

الاسم ما زال بإيموجي: «خدمات كشف التسربات والعزل 💧».

### `/category/moving-storing/` و`/category/cleaning/`

النص الجغرافي صار «في السعودية»، لكن الوصف ما زال يحتوي:

- `tel:0568060309` + إيموجي 📞
- `wa.me/971586634710`
- إيموجي في الاسم: 🚚 و 🧹🧴🧼🧽
- ادّعاءات غير مثبتة: «حماية 100% ضد الخدش»، «خصم 15% على أول طلب»

### تصنيف `/en/category/service-en/`

H1 إنجليزي `Services`، روابط فرعية مخلوطة («خدمات Roof insulation»)، وصورة `roof-insulation-ae.webp`.

---

## ج) أخطاء القالب والنواقص (ليست نصوص إمارات بالضرورة)

### 1. لا `<title>` — حرج SEO

| المسار | `<title>` | `og:title` | JSON-LD |
|---|---|---|---|
| `/` | شركة ركن التطور - السعودية | نعم | 3 |
| `/cities/` | المدن ♻ ركن التطور 🇸🇦 | نعم | 1 |
| `/sitemap/` | خريطة الموقع ♻ ركن التطور 🇸🇦 | نعم | 1 |
| `/about/` `/contact-us/` `/privacy-policy/` `/services/` `/pricing/` `/blog/` `/faq/` `/en/` | **لا** | **لا** | 0 (ما عدا `/en/` فيه 2) |
| مقالات الخدمة المختبرة (تسربات الرياض، تنظيف الرياض، شحن جدة…) | **لا** | **لا** | 4–5 |
| `/category/water-leak-detection/` | **لا** | **لا** | 0 |
| `/sa/sa/` (تكرار الرئيسية) | **لا** | **لا** | 2 |

`wp_head` يُستدعى جزئياً (canonical، RSS، oEmbed، `hide-call-early`) لكن `document_title` لا يُطبع. قالب KAYAN للصفحات الداخلية/المفرد على الأرجح يبني `<head>` يدوياً ويتخطى `wp_title` / Rank Math.

`/cities/` و`/sitemap/` ما زالتا تستخدمان عنوان Rank Math الافتراضي مع إيموجي ♻ 🇸🇦 — نفس عطل 16 سبتمبر.

### 2. `/sa/sa/` محتوى مكرر 200

يفتح نسخة ثانية من الرئيسية (~383 ك.ب) بدون `<title>`. القصاصة 12 تصلّح `#webpage` في السكيما على بعض الصفحات، لكن المسار نفسه ما زال 200. الكانونيكال يشير إلى `/sa/` — جيد نسبياً، والمسار يبقى باب فهرسة/حيرة.

### 3. تصنيف المدينة `/city/riyadh/` = 404

كان في 16 سبتمبر يعيد توجيه صامت للرئيسية؛ الآن 404. لا REST لتصنيف `city`. صفحة `/cities/` ما زالت رقيقة: excerpt «تواصل 0568060309»، ومقتطف يُقطع.

### 4. فيديو يوتيوب وهمي في قالب المقال

كل المقالات المفحوصة:

`img.youtube.com/vi/الفيديو/0.jpg`

المعرّف النصي «الفيديو» وليس ID. ودجت الفيديو في KAYAN/YouColor مفعّل بلا مصدر.

### 5. تقييمات وهمية في السكيما وودجت القالب

- الرئيسية: `AggregateRating` **4.9 / reviewCount 6481** (وسكيما ثانية `priceRange: "$$ – $$$"` بجانب `SAR`).
- المقالات: `CreativeWorkSeries` **ratingValue 5 / ratingCount 16948**.
- CPT `reviews` = **0**. لا مراجعات حقيقية تغذي الرقم.

### 6. رقم `0568060309` ما زال داخل المقالات بينما الاتصال مخفي

بحث REST: **1666** مقالاً تطابق `0568060309`.

أمثلة حية:

- `/water-leak-detection-riyadh/`: سكيما `telephone:0568060309` + زر ظاهر نصاً «اتصل الآن 0568060309» (الـ `href=tel:` يُخفى بالـ CSS فيبقى نص الهاتف).
- `/home-cleaning-riyadh/`: «تواصل على 0568060309».
- `/water-leak-detection-jeddah/`: الشورت كود `اذكر الحي والعَرَض على 0568060309`.
- صفحات `/pricing/` و`/cities/`: الرقم في المقتطف و`<title>`/meta.

النتيجة للزائر: أزرار اتصال مخفية + رقم سعودي مكتوب في النص + واتساب إماراتي في الهيدر. NAP ثلاثي.

### 7. CPT فارغة ما عدا 12 خدمة

| النوع | العدد |
|---|---|
| services | 12 |
| reviews | 0 |
| faqs | 0 |
| pricing | 0 |
| portfolio | 0 |
| before_after | 0 |

`has_archive=false` عبر القصاصة 12 يمنع أرشيف `services`/`pricing` الإنجليزي. صفحات `/services/` و`/pricing/` أصبحت صفحات WordPress عادية (id 7463 / 7464) لا بطاقات CPT.

### 8. الإنجليزية `/en/`

- `lang="en" dir="ltr"` صحيح شكلياً.
- بلا `<title>`.
- H1 مخلوط: `Rukn Eltatawer السعودية — integrated home services في مدن المملكة`.
- لا hreflang على العربية ولا على `/en/` في العيّنة (قصاصة 11 يُفترض أن تضيف 3 روابط — غير ظاهرة في HTML المفحوص).
- محتوى عربي تحت واجهة LTR.

### 9. هوية/سوشيال/بريد غير سعودية الصفحات

| القناة | القيمة الحية | مشكلة |
|---|---|---|
| فيسبوك | `facebook.com/melsaadgroup` (+ `mahmoudelsaadd` على `/cities/`) | مجموعة الأم لا صفحة SA |
| إنستغرام | `instagram.com/elsaad_group` | مجموعة الأم |
| X | `x.com/melsaad22` و`twitter.com/RuknEltatawer` (على الرئيسية/cities فقط) | حساب شخصي + حساب قديم |
| يوتيوب | `youtube.com/@rukn-eltatawer.com1` | لاحقة `.com1` |
| بريد الإعدادات | `admin@rukn-eltatawer.com` | |
| بريد السكيما/الفوتر | `m@rukn-eltatawer.com` | عدم تطابق |
| `site_logo` | `null` | الشعار ملف يدوي |
| `show_on_front` | `posts` / `page_on_front=0` | لا صفحة أمامية في إعداد ووردبريس |

### 10. أخطاء قالب أخرى ما زالت حية

- CSS: `text-transform: capitalize` على `input` (يكسر العربية في البحث).
- كلمة `Toggle` ما زالت في HTML الرئيسية (EZ-TOC؛ القصاصة 12 تحذف `eztoc-hide` فقط إذا طابق النمط).
- كلاسات بائع: `YourColor`, `yourcolor--copyright`, `shadwo`/`defualt` التاريخية.
- فوتر عام بإيموجي على أرشيفات التصنيف: «كشف تسربات المياه💧🕵️‍♂️عزل الأسطح🏠…».
- شركاء الرئيسية (Sika/Fosroc/Jotun/Weber) نص بلا شعارات حقيقية كما سبق.
- `dump-keys` **مفتوح بدون تسجيل: HTTP 200** ويعيد واتساب وإعدادات الصفحة الرئيسية. في نسخة المستودع القصاصة 15 تقيّده بـ `manage_options` — النسخة الحية لا تطابق المستودع (أو قصاصة أخرى تعيد تسجيل المسار بدون صلاحية). DNI العام `/kayan/v1/dni` 200: `phone:""` / `wa_number:971586634710`.

### 11. صفحات تسويقية بأسلوب «نعم، … هو الحل النهائي»

`/about/` `/contact-us/` `/privacy-policy/` `/sitemap/` `/faq/` ما زالت مقالات حشو 2026 مع H1 قصير ثم H2 طويل مكرر، وEZ-TOC، وادّعاءات («مصرح بها من وزارة الصحة» في من نحن). ليست بقايا إمارات حرفياً لكنها قالب محتوى مستنسخ.

### 12. تصنيفات يتيمة/مختصرة

`/category/leak/` `/insul/` `/maint/` `/clean/` عناوين قصيرة بلا وصف.  
`/category/home-cleaning/` و`/category/ac-maintenance/` = **404**.  
تصنيف `service` يضم **1756** مقالاً تحت اسم عام «الخدمات».

---

## د) ما ليس خطأ (حتى لا يُخلط)

| البند | الحكم |
|---|---|
| واتساب `971586634710` في الهيدر/الفوتر/FAB/RuknCS | سياسة مؤقتة بطلبكم |
| إخفاء `tel:` و`call_show:false` | سياسة مؤقتة |
| ظهور كلمة «مصر» في محوّل الدول | شبكة دول، ليست نسخة مصرية للمحتوى |
| `01151481000` داخل دالة JS `protect()` | حماية من أرقام مصرية قديمة، غير ظاهرة للزائر |
| بحث REST عن «دبي» أصاب مقالات الشحن | تطابق فرعي داخل كلمة **«أدبي»** — ليست ذكراً لدبي |
| كلاسات `YourColor` في CSS | اسم المنتج الأصلي، لا يراه الزائر كنص دولة |

---

## هـ) طبقة الترقيع الحالية (7 قصاصات نشطة من 15)

| # | الحالة | الاسم |
|---|---|---|
| 5 | نشط | Rukn REST Meta Bridge for Article Blocks |
| 7 | نشط | Rukn Fix Site Identity Schema Phones |
| 8 | نشط | Rukn SA Google Indexing Fix (robots + schema) |
| 11 | نشط | Rukn SA EN/AR Hreflang (core pages) — غير ظاهر أثره في HTML |
| 12 | نشط | Rukn SA Frontend Copy Guard (ob_start) |
| 14 | نشط | Rukn SA Index Ready Hardening |
| 15 | نشط | Rukn SA Trust NAP Fix |

القالب نفسه (`wp-content/themes/kayan-theme`) **ليس في هذا المستودع**. كل «الإصلاح» طبقة PHP أمام القالب. لذلك AED/5٪، علم الإمارات، `<title>`، فيديو «الفيديو»، و`ratingCount` 16948 لن تختفي إلا من مصدر القالب أو خياراته.

---

## ترتيب إصلاح مقترح (عند الطلب فقط)

1. **`<title>` + `og:title`** على قوالب page/single/archive في KAYAN (أو إجبار Rank Math عبر `wp_head` كامل). إزالة ♻ من عناوين المدن/الخريطة.
2. **عملة القالب:** `SAR` و`taxRate` 15 (أو إخفاء الحاسبة) في `KayanBookingConfig` وبوابة tanceq و`data-currency` للباقات.
3. **تنظيف أوصاف التصنيفات من المصدر** (ليس فقط HTML): sewer-drainage، leak-detection-and-insulation، moving-storing، cleaning — حذف دبي/أبوظبي/`971568060309`/tel السعودي الظاهر.
4. علم الإمارات في `ksw-country-btn`، وإخفاء زر AE إن لم تكن هناك نسخة إمارات للزائر السعودي.
5. حذف ودجت يوتيوب `vi/الفيديو` وتقييم 5/16948 من قالب المقال؛ تقييم الرئيسية 4.9/6481.
6. توحيد NAP في أجسام المقالات: إما شطب `0568060309` من 1666 مقالاً أو إبقاؤه مخفياً مع واتساب فقط — الوضع الحالي يناقض نفسه.
7. إغلاق `dump-keys` الحي ليطابق صلاحية المدير في المستودع.
8. 301 لـ `/sa/sa/`، إصلاح `/city/riyadh/` أو حذف الروابط إليه، و`roof-insulation-ae.webp`.
9. سوشيال/بريد صفحة السعودية لا حسابات المجموعة الأم.
10. `/en/`: إما ترجمة حقيقية أو noindex.

لم يُمسّ الموقع الحي في هذا الفحص.
