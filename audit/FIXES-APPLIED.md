# ما تم إصلاحه على الموقع الحي — 16 سبتمبر 2026

تم التطبيق مباشرة على https://www.rukn-eltatawer.com/sa عبر REST وCode Snippets. **لم يُرفع رقم اتصال سعودي.** واتساب موحّد على الرقم الإماراتي مؤقتاً، وأزرار الاتصال مخفية حتى يتوفر رقم سعودي.

## سياسة التواصل الحالية

| القناة | القيمة |
|---|---|
| واتساب (كل الأزرار والهيدر والفوتر والـ FAB وDNI) | `+971586634710` / `https://wa.me/971586634710` |
| أزرار الاتصال / `tel:` / `call_show` | **مخفية** (`call_show:false`, `hide__floating__call=1`, CSS يخفي `tel:`) |
| `dump-keys` العام | أصبح يتطلب صلاحية مدير (401 بدون تسجيل) |

عند توفر رقم سعودي: حدّث `whatsapp_number` و`kayan_country_sa_whatsapp` إن لزم، وأعد تفعيل `kayan_show_call_buttons` وأزل إخفاء `tel:` من القصاصة 12.

## قصص Code Snippets المحدّثة

- **#15 Rukn SA Trust NAP Fix:** يضبط خيارات القالب (واتساب إماراتي، إخفاء الاتصال، هوية سعودية، إحصاءات 12/18/10).
- **#12 Rukn SA Frontend Copy Guard:** توحيد واتساب، حذف/تحويل روابط الهاتف، استبدال نصوص الإمارات، تعطيل أرشيف CPT الذي كان يحجب صفحات الخدمات/الأسعار، تنظيف أوصاف التصنيفات، إخفاء CSS لأزرار الاتصال، إصلاح `/sa/sa/` في السكيما، إيقاف تحويل لغة المتصفح الذي سبب حلقة توجيه.

## محتوى الصفحات

صفحات: اتصل بنا، من نحن، الخصوصية، الخدمات، الأسعار، المدن، الأسئلة، الخريطة، المدونة.

- حذف `+966000000000` وروابط الماركداون المعطوبة.
- تحويل أزرار `tel:` داخل المحتوى إلى واتساب أو إخفائها.
- بطاقة الخصوصية: «في السعودية» بدل «في الإمارات».
- إزالة تكرار «في السعودية في السعودية».
- H1 واحد لكل صفحة (العناوين الداخلية صارت H2).

## قوائم

روابط القائمة الرئيسية لم تعد `#services` / `#projects` الفارغة، وأصبحت صفحات حقيقية (`/services/`, `/cities/`, `/blog/`, `/about/`, `/faq/`).

## تحقق بعد الإصلاح

- الرئيسية والمتصفح الإنجليزي: HTTP 200 بدون حلقة توجيه.
- واتساب الوحيد في الصفحات المفحوصة: `971586634710`. لا روابط `tel:` في الهيدر.
- `/services/` و`/pricing/` تعرضان محتوى عربي لا أرشيف إنجليزي فارغ.
- تصنيفات التسربات/المجاري لم تعد تذكر دبي وأبوظبي.
- `/en/` لم يعد يُجبر بتحويل المتصفح (كان يسبب `ERR_TOO_MANY_REDIRECTS` مع Chrome الإنجليزي).

## ما تبقّى ويحتاج عملاً لاحقاً

- شعارات قسم الشركاء (ملفات صور غير مرفوعة).
- تقييمات وهمية في ودجت القالب (5841/98.7%) وCPT التقييمات ما زال فارغاً.
- كتالوج المدن العشر شبه الفارغ، ومقالات FAQ القالبية (غسيل مكيفات/شحن…).
- حساب تويتر 404، فيسبوك مجموعة الأم.
- عناوين Rank Math ما زالت تستخدم إيموجي ♻ على بعض الصفحات.
- ترجمة إنجليزية حقيقية لـ `/sa/en/` (أُوقف التحويل القسري فقط).
